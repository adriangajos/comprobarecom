rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Orders are written only by the Cloud Function (Admin SDK bypasses rules).
    // No public client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
